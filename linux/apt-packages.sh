#!/usr/bin/env bash
# Install packages that are commonly available via APT in the distros I use

sudo apt install fish mosh iotop fzf bfs

# needed for some Cargo packages
sudo apt install llvm libclang-dev