- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.vimrc: vimrc
    ~/scripts: scripts/linux
    ~/notes: notes
    ~/.oh-my-zsh: .oh-my-zsh
    ~/.powerlevel10k: powerlevel10k
# sort out zsh customization later, some open questions
#    ~/.zshrc: zshrc

- create:
    - ~/downloads

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [chsh -s $(which zsh), Making zsh the default shell]